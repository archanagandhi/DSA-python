arr = [11, 9, 29, 7, 2, 15, 28]
for i in range(1, len(arr)):
    while(i > 0 and arr[i] < arr[i - 1]):
        arr[i], arr[i - 1] = arr[i - 1], arr[i]
        i -= 1
while
    list=[]


    if len(arr)%2==1:
    mid_index = (0+len(arr))//2
    mid=arr[mid_index]
else:
    pass
print(arr)
print(mid)

